<#
{
    "Category": 4,
    "Name": "Auto Counsel",
    "Info": "Auto-prepare counseling for scanned prescriptions at Will Call. [Start on Will Call screen after scanning Rxs]",
    "Shortcut": "F2",
    "Code": "113",
    "Window": "Minimized",
    "Programs": "java.exe"
}
#>

# Set the working directory to the script's location
$scriptDir = Split-Path -Path $MyInvocation.MyCommand.Definition -Parent
Set-Location -Path $scriptDir

# Load variables from precheck.ps1
. .\precheck.ps1

# Check the exit code of the last command
if ($LASTEXITCODE -ne 0) {
    . .\errorHandler.ps1 $global:precheckError
    exit 1
}

# List of variables to check
$variables = @{
    "Main" = $Main
    "Clavier" = $Clavier
    "Cap2Txt" = $Cap2Txt
    "SendKeys" = $SendKeys
    "Printer" = $Printer
}

# Check if all necessary variables are defined
foreach ($var in $variables.GetEnumerator()) {
    if (-not $var.Value) {
        $errorMessage = "Error: '$($var.Key)' is not defined.`nOpen Setup [Shift + F1]"
        . ./errorHandler.ps1 $errorMessage
        exit 1
    }
}

# Load required assemblies
Add-Type -AssemblyName System.Windows.Forms
Add-Type -AssemblyName System.Drawing
Add-Type -AssemblyName Microsoft.VisualBasic

# ====================================================================================
# UTILITY FUNCTIONS - Color Detection & Classification
# ====================================================================================

# 1) Safe, disposable pixel reader with optional neighborhood averaging
function Get-ScreenColor {
    [CmdletBinding(DefaultParameterSetName='Cursor')]
    param(
        [Parameter(Mandatory=$true, ParameterSetName='Point')][int]$X,
        [Parameter(Mandatory=$true, ParameterSetName='Point')][int]$Y,
        [int]$Kernel = 0,   # 0=single pixel; 1=3x3; 2=5x5 (averaged)
        [switch]$Raw       # return [System.Drawing.Color] if set; otherwise "rgb(r, g, b)"
    )

    if ($PSCmdlet.ParameterSetName -eq 'Cursor') {
        $p = [System.Windows.Forms.Cursor]::Position
        $X = $p.X; $Y = $p.Y
    }

    $w = $h = (2 * $Kernel + 1)
    $bmp = New-Object System.Drawing.Bitmap($w, $h)
    try {
        $g = [System.Drawing.Graphics]::FromImage($bmp)
        try {
            $g.CopyFromScreen(
                [System.Drawing.Point]::new($X - $Kernel, $Y - $Kernel),
                [System.Drawing.Point]::Empty,
                [System.Drawing.Size]::new($w, $h)
            )
        } finally { $g.Dispose() }

        if ($Kernel -le 0) {
            $c = $bmp.GetPixel([Math]::Min($Kernel,$w-1), [Math]::Min($Kernel,$h-1))
        } else {
            $sumR = $sumG = $sumB = 0
            for ($yy=0; $yy -lt $h; $yy++) {
                for ($xx=0; $xx -lt $w; $xx++) {
                    $p = $bmp.GetPixel($xx,$yy)
                    $sumR += $p.R; $sumG += $p.G; $sumB += $p.B
                }
            }
            $px = $w * $h
            $r = [int][Math]::Round($sumR / $px)
            $gC = [int][Math]::Round($sumG / $px)
            $b = [int][Math]::Round($sumB / $px)
            $c = [System.Drawing.Color]::FromArgb($r,$gC,$b)
        }

        if ($Raw) { return $c }
        else { return ("rgb({0}, {1}, {2})" -f $c.R, $c.G, $c.B) }
    }
    finally { $bmp.Dispose() }
}

# 2) Wait until the cursor stops moving (so we don't sample mid-move)
function Wait-CursorStable {
    param(
        [int]$StableMs = 100,
        [int]$PollMs   = 20,
        [int]$TimeoutMs= 2000
    )
    $deadline = (Get-Date).AddMilliseconds($TimeoutMs)
    $stableFor = 0
    $last = [System.Windows.Forms.Cursor]::Position
    while ((Get-Date) -lt $deadline) {
        Start-Sleep -Milliseconds $PollMs
        $now = [System.Windows.Forms.Cursor]::Position
        if ($now.X -eq $last.X -and $now.Y -eq $last.Y) {
            $stableFor += $PollMs
            if ($stableFor -ge $StableMs) { return $now }
        } else {
            $last = $now; $stableFor = 0
        }
    }
    return $null
}

# 3) Color math + majority classification with tolerance ("near green" support)
function Get-ColorDistance([System.Drawing.Color]$a, [System.Drawing.Color]$b) {
    $dr = $a.R - $b.R; $dg = $a.G - $b.G; $db = $a.B - $b.B
    [Math]::Sqrt($dr*$dr + $dg*$dg + $db*$db)
}

function Test-ColorBucket {
    param(
        [System.Drawing.Color[]]$Samples,
        [hashtable]$Targets,   # e.g., @{ Green = [Drawing.Color]::FromArgb(0,178,0) }
        [hashtable]$Skips,     # e.g., @{ Black=[...] ; Gray=[...] }
        [int]$Tolerance = 18
    )
    $hit = @{}; foreach($k in $Targets.Keys){ $hit[$k]=0 }
    $miss= @{}; foreach($k in $Skips.Keys){   $miss[$k]=0 }

    foreach($s in $Samples){
        foreach($k in $Targets.Keys){ if((Get-ColorDistance $s $Targets[$k]) -le $Tolerance){ $hit[$k]++ } }
        foreach($k in $Skips.Keys){   if((Get-ColorDistance $s $Skips[$k])   -le $Tolerance){ $miss[$k]++ } }
    }
    
    $n = [Math]::Max(1,$Samples.Count)
    $bestHitName=$null; $bestHit=-1
    foreach($k in $hit.Keys){ if($hit[$k] -gt $bestHit){ $bestHit=$hit[$k]; $bestHitName=$k } }
    
    $bestMissName=$null; $bestMiss=-1
    foreach($k in $miss.Keys){ if($miss[$k]-gt $bestMiss){ $bestMiss=$miss[$k]; $bestMissName=$k } }

    if ($bestHit  -ge [Math]::Ceiling($n/2.0)) { return @{ Result='Success'; Name=$bestHitName;  Count=$bestHit } }
    if ($bestMiss -ge [Math]::Ceiling($n/2.0)) { return @{ Result='Skip';    Name=$bestMissName; Count=$bestMiss } }
    return @{ Result='Unknown'; Name=$null; Count=0 }
}

# 4) Debounced wait for a pixel to classify as Success/Skip with multi-sampling
function Wait-Pixel {
    param(
        [int]$X, [int]$Y,
        [hashtable]$Targets,
        [hashtable]$Skips,
        [int]$Tolerance = 70,
        [int]$StableReads = 1,
        [int]$SamplesPerRead = 1,
        [int]$Kernel = 1,
        [int]$InterSampleMs = 10,
        [int]$SettleMs = 10,
        [int]$TimeoutMs = 200,
        [switch]$LogRaw
    )
    $deadline = (Get-Date).AddMilliseconds($TimeoutMs)
    $stable = 0
    $iter = 0

    while ((Get-Date) -lt $deadline) {
        Start-Sleep -Milliseconds $SettleMs
        $iter++

        # Collect samples
        $samples = @()
        for ($i=0; $i -lt $SamplesPerRead; $i++) {
            $samples += (Get-ScreenColor -X $X -Y $Y -Raw -Kernel $Kernel)
            if ($i -lt $SamplesPerRead-1) { Start-Sleep -Milliseconds $InterSampleMs }
        }

        # Classify
        $cls = Test-ColorBucket -Samples $samples -Targets $Targets -Skips $Skips -Tolerance $Tolerance

        # --- Minimal console log (only when -LogRaw is passed) ---
        if ($LogRaw) {
            $rgb = ($samples | ForEach-Object { "($($_.R),$($_.G),$($_.B))" }) -join ', '
            Write-Host ("Wait-Pixel iter={0} @({1},{2}) kernel={3} tol={4} samples={5} rgb={6}" -f $iter, $X, $Y, $Kernel, $Tolerance, $SamplesPerRead, $rgb)
            Write-Host ("  classify => result={0} name={1} count={2} stable={3}" -f $cls.Result, $cls.Name, $cls.Count, $stable)
        }
        # ---------------------------------------------------------

        switch ($cls.Result) {
            'Success' {
                $stable++
                if ($stable -ge $StableReads) {
                    if ($LogRaw) { Write-Host ("  -> RETURN Success (X={0}, Y={1}, Bucket={2})" -f $X, $Y, $cls.Name) }
                    return @{ Result='Success'; X=$X; Y=$Y; Bucket=$cls.Name }
                }
            }
            'Skip' {
                if ($LogRaw) { Write-Host ("  -> RETURN Skip (X={0}, Y={1}, Bucket={2})" -f $X, $Y, $cls.Name) }
                return @{ Result='Skip'; X=$X; Y=$Y; Bucket=$cls.Name }
            }
            default {
                $stable = 0
            }
        }
    }

    if ($LogRaw) { Write-Host ("  -> RETURN Timeout (X={0}, Y={1})" -f $X, $Y) }
    return @{ Result='Timeout'; X=$X; Y=$Y }
}

# Helper function to safely activate a window with retry logic
function Activate-WindowSafe {
    param(
        [string]$WindowTitle,
        [int]$RetryCount = 2,
        [int]$RetryDelayMs = 80
    )
    
    for ($i = 0; $i -lt $RetryCount; $i++) {
        try {
            $result = [Microsoft.VisualBasic.Interaction]::AppActivate($WindowTitle)
            if ($result) { return $true }
        } catch {
            # Window not found, continue to retry
        }
        
        if ($i -lt ($RetryCount - 1)) {
            Start-Sleep -Milliseconds $RetryDelayMs
        }
    }
    
    return $false
}

# ====================================================================================
# MAIN SCRIPT LOGIC
# ====================================================================================

# Move EPS to the side
& $Clavier /sendkeys "[{Focus,0,*EPS/*}][{MouseMoveToFocus,0,0}][{MouseButton,LD}][{MouseMoveTo,880,250}][][{MouseButton,LU}]"
Start-sleep -Milliseconds 100

& $Clavier /sendkeys "[{Focus,0,*EPS/*}][{MouseMoveToFocus,620,140}][{MouseButton,L}][][Ctrl+A][Backspace][{Focus,0,*EPS/*}]"
Start-sleep -Milliseconds 300

[System.Windows.Forms.SendKeys]::SendWait("{TAB}")
[System.Windows.Forms.SendKeys]::SendWait("{PGUP 2}")

# Initialize counters
$Total = 0
$Sell = 0
$Required = 0
$PreviousRx = ""
$done = $false
$CurrentRxStatus = ""
$PreviousRxKey = $null
$DupCount = 0

# Read through the prescription list
Do {
    # Slightly slower copy + quick fallback reread (tiny delay for stability)
    [System.Windows.Forms.SendKeys]::SendWait("^c")
    Start-Sleep -Milliseconds 25
    $clip = Get-Clipboard
    if (-not $clip) { Start-Sleep -Milliseconds 30; $clip = Get-Clipboard }

    $cols = $clip.Split("`t")
    if ($cols.Count -lt 14) {
        # One more light retry if the row text wasn't ready yet
        Start-Sleep -Milliseconds 30
        [System.Windows.Forms.SendKeys]::SendWait("^c")
        Start-Sleep -Milliseconds 25
        $clip = Get-Clipboard
        $cols = $clip.Split("`t")
        if ($cols.Count -lt 14) {
            # If still malformed, step down and try next row (do not count this one)
            [System.Windows.Forms.SendKeys]::SendWait("{DOWN}")
            Start-Sleep -Milliseconds 25
            continue
        }
    }

    $CurrentRxStatus = $cols[13]
    $CurrentRx       = $cols[0,4,5]
    $CurrentRequired = $cols[9]
    $CurrentRxSell   = $cols[1]
    $CurrentRxKey    = ($cols[0],$cols[4],$cols[5]) -join '|'

    # Duplicate detection: allow up to 2 dupes; stop only on the 3rd in a row
    if ($PreviousRxKey -and $PreviousRxKey -eq $CurrentRxKey) {
        $DupCount++
    } else {
        $DupCount = 0
    }

    if ($DupCount -ge 3) {
        $done = $true
        break
    }

    # If duplicate but not 3rd yet, skip counting and move on
    if ($DupCount -gt 0) {
        [System.Windows.Forms.SendKeys]::SendWait("{DOWN}")
        Start-Sleep -Milliseconds 25
        continue
    }

    if ([string]$CurrentRxSell -eq "True") {
        $Sell++
        Start-Sleep -Milliseconds 8
        if ([string]$CurrentRequired -match "Required") {
            $Required++
        }
    }

    $Total++
    $PreviousRxKey = $CurrentRxKey
    Write-Host "$Total $CurrentRx"

    [System.Windows.Forms.SendKeys]::SendWait("{DOWN}")
    Start-Sleep -Milliseconds 25
    Write-Host "System-Send-Key: {DOWN}"
} Until ($done)

Write-Host "Total: $Total"
Write-Host "Sell: $Sell"
Write-Host "Required: $Required"

$Scripts = $Sell

if ($Sell -eq 0) {
    EXIT
}

if ($CurrentRxStatus -match "Halt") {
    [System.Windows.Forms.SendKeys]::SendWait("{UP}")
    Write-Host "System-Send-Key: {UP}"
}

[Microsoft.VisualBasic.Interaction]::AppActivate("Enterprise")
[System.Windows.Forms.SendKeys]::SendWait("%{o}")
Start-sleep -Milliseconds 150

Write-Host "==== PRE-CHECK ===="

$MTMSuccess = 0
$MTMSkip    = 0

# --- MTM pixel check (ADMIN PARAMETERS: tight tolerance, fast response) ---
& $Clavier /sendkeys "[{Focus,0,*EPS/*}][{MouseMoveToFocus,701,692}]"
Start-Sleep -Milliseconds 40

$anchor = Wait-CursorStable -StableMs 50 -PollMs 25 -TimeoutMs 500
if (-not $anchor) {
    $MTMSkip = 1
} else {
    $targets = @{ MTMGreen = [System.Drawing.Color]::FromArgb(0,178,0) }
    $skips   = @{
        Black = [System.Drawing.Color]::FromArgb(0,0,0)
        Gray  = [System.Drawing.Color]::FromArgb(168,170,168)
        Gray2 = [System.Drawing.Color]::FromArgb(178,179,178)
    }

    Write-Host "#CHECK MTM"
    # ADMIN PARAMETERS: Very tight tolerance (5) for green detection
    $check = Wait-Pixel -X $anchor.X -Y $anchor.Y -Targets $targets -Skips $skips -Tolerance 5 -StableReads 2 -SamplesPerRead 1 -Kernel 1 -SettleMs 10 -TimeoutMs 900

    & $Clavier /sendkeys "[{Focus,0,*EPS/*}][{MouseMoveToFocus,701,692}]"
    Start-Sleep -Milliseconds 40

    switch ($check.Result) {
        'Success' { $MTMSuccess = 1; Write-Host "MTM Success!" }
        'Skip'    { $MTMSkip    = 1 }
        'Timeout' { $MTMSkip    = 1 }
    }
}

Start-Sleep -Milliseconds 300

if (-not (Activate-WindowSafe "Information")) {
    & $Clavier /sendkeys "[{Focus,0,Information*}][ENTER]"
    Start-sleep -Milliseconds 250
}
if (-not (Activate-WindowSafe "Message")) {
    & $Clavier /sendkeys "[{Focus,0,Message*}][ENTER]"
    Start-sleep -Milliseconds 250
}
if (-not (Activate-WindowSafe "Counseling")) {
    & $Clavier /sendkeys "[{Focus,0,Counseling*}][ENTER][][{Focus,0,Message*}][ENTER][]"
    Start-sleep -Milliseconds 250
}

if ($MTMSuccess -eq 1) {
    Start-sleep -Milliseconds 300
    & $Clavier /sendkeys "[{Focus,0,MTM*}][Alt+C]"
    Start-sleep -Milliseconds 250
}

Start-sleep -Milliseconds 200

& $Clavier /sendkeys "[{Focus,0,*EPS/*}][{MouseMoveToFocus,29,385}]"
Start-Sleep -Milliseconds 10

# ====================================================================================
# MAIN COUNSELING LOOP - OPTIMIZED with Tuned Parameters
# ====================================================================================

# Loop state
$CheckStop = 0
$Processed = 0
$LoopCount = 0

# Safety guards
$OverrunBudget = 2
$MaxLoopsAllowed = $Sell + $OverrunBudget + 3

$badReadConsecutive     = 0
$badReadConsecutiveMax  = 12

$loadingConsec    = 0
$loadingConsecMax = 6

$UnknownExitAfter  = 2
$unknownStreak     = 0

Write-Host "Loop Configuration: Sell=$Sell, MaxLoopsAllowed=$MaxLoopsAllowed"

while ($true) {

    if ($Processed -ge $Sell) {
        if ($LoopCount -gt ($Sell + 5)) {
            Write-Host "All items processed. Exiting cleanly."
            break
        }
    } elseif ($LoopCount -gt $MaxLoopsAllowed) {
        Write-Host "SAFETY: Hard loop cap reached. Exiting."
        EXIT 408
    }
    
    $LoopCount++

    # Always move to the canonical status pixel
    & $Clavier /sendkeys "[{Focus,0,*EPS/*}][{MouseMoveToFocus,29,385}]"
    Start-Sleep -Milliseconds 10

    # Lock a fresh absolute point each pass
    $pt = Wait-CursorStable -StableMs 40 -PollMs 20 -TimeoutMs 300
    if (-not $pt) { $pt = [System.Windows.Forms.Cursor]::Position }

    # ===========================================================================================
    # COLOR BUCKET DEFINITION - Tuned for Nuanced Detection
    # ===========================================================================================
    $targets = @{ Ready = [System.Drawing.Color]::FromArgb(7,165,5) }
    $skips   = @{
        White    = [System.Drawing.Color]::FromArgb(251,251,251)
        Gray     = [System.Drawing.Color]::FromArgb(168,170,168)
        GrayMid  = [System.Drawing.Color]::FromArgb(200,200,200)
        GrayLite = [System.Drawing.Color]::FromArgb(212,212,212)
        Loading  = [System.Drawing.Color]::FromArgb(241,243,241)
    }

    # ===========================================================================================
    # ADAPTIVE PARAMETER TUNING based on Admin Script Analysis
    # ===========================================================================================
    
    if ($Processed -lt $Sell) {
        # NORMAL MODE: Still processing items
        $StatusTolerance      = 15
        $StatusStableReads    = 2
        $StatusSamplesPerRead = 1
        $StatusKernel         = 1
        $timeoutMs            = 400
    } else {
        # OVERRUN MODE: Already processed all items
        $StatusTolerance      = 10
        $StatusStableReads    = 2
        $StatusSamplesPerRead = 1
        $StatusKernel         = 1
        $timeoutMs            = 300
    }

    # Classify the pixel
    $cls = Wait-Pixel -X $pt.X -Y $pt.Y -Targets $targets -Skips $skips -Tolerance $StatusTolerance -StableReads $StatusStableReads -SamplesPerRead $StatusSamplesPerRead -Kernel $StatusKernel -SettleMs 10 -TimeoutMs $timeoutMs

    Write-Host "[Loop $LoopCount | $Processed/$Sell] $($cls.Result) / $($cls.Bucket)"

    # Track unknown streak
    if ($cls.Result -eq 'Unknown' -or $cls.Result -eq 'Timeout') {
        $unknownStreak++
        if ($unknownStreak -ge $UnknownExitAfter) {
            Write-Host "SAFETY: Unbucketed color. Exiting."
            EXIT 408
        }
    } else {
        $unknownStreak = 0
    }

    # Track bad reads
    if ($cls.Result -in @('Success','Skip')) { $badReadConsecutive = 0 } else { $badReadConsecutive++ }

    if ($badReadConsecutive -ge $badReadConsecutiveMax) {
        Write-Host "SAFETY: Too many bad reads. Exiting."
        EXIT 408
    }

    # ===========================================================================================
    # STATE MACHINE - Optimized for Speed
    # ===========================================================================================

    if ($cls.Result -eq 'Success') {
        # GREEN => STOP (use fast exit)
        Write-Host "#GREEN"
        $loadingConsec = 0
        $CheckStop = 1
        break
    }
    elseif ($cls.Result -eq 'Skip' -and $cls.Bucket -eq 'White') {
        # WHITE => Process next item (FASTEST PATH)
        Write-Host "#WHITE ($Processed -> $($Processed+1))"
        $loadingConsec = 0

        # Execute Alt+l and Alt+x quickly
        & $Clavier /sendkeys "[{Focus,0,*EPS/*}][Alt+l][][Alt+x]"
        Start-Sleep -Milliseconds 200
        $Processed++

        # Quick popup handling (no retry, just attempt)
        if (-not (Activate-WindowSafe "Information" 1 50)) {
            & $Clavier /sendkeys "[{Focus,0,Information*}][ENTER]"
            Start-Sleep -Milliseconds 150
        }
        if (-not (Activate-WindowSafe "Message" 1 50)) {
            & $Clavier /sendkeys "[{Focus,0,Message*}][ENTER]"
            Start-Sleep -Milliseconds 150
        }
        if (-not (Activate-WindowSafe "Counseling" 1 50)) {
            & $Clavier /sendkeys "[{Focus,0,Counseling*}][ENTER][][{Focus,0,Message*}][ENTER][]"
            Start-Sleep -Milliseconds 150
        }

        continue
    }
    elseif ($cls.Result -eq 'Skip' -and ($cls.Bucket -in @('Gray','GrayMid','GrayLite'))) {
        # GRAY => Nudge (MEDIUM SPEED PATH)
        Write-Host "#GRAY"
        $loadingConsec = 0

        & $Clavier /sendkeys "[{Focus,0,*EPS/*}][Alt+x]"
        Start-Sleep -Milliseconds 120

        # Quick popup check
        if (-not (Activate-WindowSafe "Information" 1 40)) {
            & $Clavier /sendkeys "[{Focus,0,Information*}][ENTER]"
            Start-Sleep -Milliseconds 100
        }
        if (-not (Activate-WindowSafe "Message" 1 40)) {
            & $Clavier /sendkeys "[{Focus,0,Message*}][ENTER]"
            Start-Sleep -Milliseconds 100
        }
        if (-not (Activate-WindowSafe "Counseling" 1 40)) {
            & $Clavier /sendkeys "[{Focus,0,Counseling*}][ENTER][][{Focus,0,Message*}][ENTER][]"
            Start-Sleep -Milliseconds 100
        }

        continue
    }
    elseif ($cls.Result -eq 'Skip' -and $cls.Bucket -eq 'Loading') {
        # LOADING => Close MTM (SLOW PATH - needs more time)
        $loadingConsec++
        Write-Host "#LOADING ($loadingConsec/$loadingConsecMax)"

        & $Clavier /sendkeys "[{Focus,0,MTM*}][Alt+C]"
        Start-Sleep -Milliseconds 150

        try { 
            [Microsoft.VisualBasic.Interaction]::AppActivate("MTM") | Out-Null
            [System.Windows.Forms.SendKeys]::SendWait("%C")
        } catch {}

        Start-Sleep -Milliseconds 120

        if ($loadingConsec -ge $loadingConsecMax) {
            Write-Host "SAFETY: Loading timeout. Exiting."
            EXIT 408
        }

        continue
    }
    else {
        # UNKNOWN/TIMEOUT => Conservative nudge (SLOW PATH)
        Write-Host "#UNKNOWN"
        $loadingConsec = 0

        & $Clavier /sendkeys "[{Focus,0,*EPS/*}][Alt+x]"
        Start-Sleep -Milliseconds 120

        # Minimal popup check
        if (-not (Activate-WindowSafe "Information" 1 40)) {
            & $Clavier /sendkeys "[{Focus,0,Information*}][ENTER]"
            Start-Sleep -Milliseconds 100
        }
        if (-not (Activate-WindowSafe "Message" 1 40)) {
            & $Clavier /sendkeys "[{Focus,0,Message*}][ENTER]"
            Start-Sleep -Milliseconds 100
        }
        if (-not (Activate-WindowSafe "Counseling" 1 40)) {
            & $Clavier /sendkeys "[{Focus,0,Counseling*}][ENTER][][{Focus,0,Message*}][ENTER][]"
            Start-Sleep -Milliseconds 100
        }

        continue
    }
}

# ====================================================================================
# POST-LOOP CLEANUP
# ====================================================================================

Write-Host "Loop exited. Processed=$Processed/$Sell"

if ($CheckStop -eq 1) {
    if ($Required -eq 0) {
        Write-Host "No required items."
        EXIT 0
    }
    & $Clavier /sendkeys "[{Focus,0,*EPS/*}][Alt+C]"
    Start-sleep -Milliseconds 400
}
else {
    Write-Host "Loop ended abnormally."
    EXIT 408
}

if ($Required -eq 0) {
    Write-Host "Counseling complete."
    EXIT 0
}

# Play audio alert
function Set-AudioLevel($audioLevel) { 
    $wshShell = new-object -com wscript.shell
    1..50 | % { $wshShell.SendKeys([char]174) }
    $audioLevel = $audioLevel / 2
    1..$audioLevel | % { $wshShell.SendKeys([char]175) }
}

Set-AudioLevel 100

if (-not ([Microsoft.VisualBasic.Interaction]::AppActivate("Enterprise Pharmacy System"))) {
    $Audio = $Main + "Assets\audio\Default.wav"
    try {
        (New-Object Media.SoundPlayer $Audio).PlaySync()
    } catch {}
}

EXIT 0
